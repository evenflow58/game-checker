import { GoogleButtonDirective } from './google-button.directive';

describe('GoogleButtonDirective', () => {
  it('should create an instance', () => {
    const directive = new GoogleButtonDirective();
    expect(directive).toBeTruthy();
  });
});
